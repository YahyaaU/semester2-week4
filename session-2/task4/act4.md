# fgets() and sscanf()

Write a program using `scanf()` to read 3 integer values from user input in the form `3 + 4 = 7`

- we should use 3 integer variables to store the integer data
- we can provide a format string the matches the expected form of the input
    - eg. if we expected user input  


Write the same program using `fgets()` and `sscanf()`
- similar code can be used for `sscanf()` as `scanf()`
